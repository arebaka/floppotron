#pragma once
#include <Arduino.h>

typedef uint32_t Time;
typedef uint8_t Velocity;
typedef uint8_t NChannel;
typedef uint8_t NInstrument;
typedef uint8_t NPin;
typedef uint8_t NPlayingNote;
